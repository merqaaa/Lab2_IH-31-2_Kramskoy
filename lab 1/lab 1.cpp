#include <stdio.h>

int main() {
    int a, b;

    // Зчитуємо два числа з перев?ркою
    printf("Введ?ть два числа: ");
    if (scanf_s("%d %d", &a, &b) != 2) {
        printf("Помилка вводу!\n");
        return 1;  // Завершуємо програму, якщо введення некоректне
    }

    // М?няємо числа м?сцями
    a = a + b;
    b = a - b;
    a = a - b;

    // Виводимо результат
    printf("П?сля обм?ну: a = %d, b = %d\n", a, b);

    return 0;
}
